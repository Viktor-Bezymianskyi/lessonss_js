'use strict';

/* //let x = 5; alert( x++ );

//[] + false - null + true // 'NaN'

console.log(typeof([] + false)); // 'false'

let y = 1; let x = y = 2; alert(x); // последовательное присваивание с права на лево

console.log();

Чему равна сумма [ ] + 1 + 2 // '12'

Что выведет этот код: alert( "1"[0] ) // 1

Чему равно 2 && 1 && null && 0 && undefined // И запинается на лжи false

Есть ли разница между выражениями? !!( a && b ) и (a && b) // !! возвращает булиновое значение
                                        3
Что выведет этот код: alert( null || 2 && 3 || 4 );
// Или запинается на правде


a = [1, 2, 3]; b = [1, 2, 3]; Правда ли что a == b 

Что выведет этот код: alert( +"Infinity" ); // Infinity тип данных число

Верно ли сравнение: "Ёжик" > "яблоко" // false см таблицу юникода

Чему равно 0 || "" || 2 || undefined || true || falsе // 2 */

/* function Person(first, last, age, gender, interests) {

	// Определения методов и свойств
	this.name = {
		'first': first,
		'last' : last
	};
	this.age = age;
	this.gender = gender;
	//...см. Введение в объекты для полного определения
}

let person1 = new Person('Bob', 'Smith', 32, 'male', ['music', 'skiing']);
 */